/**
 * Barrel seguro para cliente de `@mario/core`: utilidades, constantes y
 * esquemas Zod. No incluye Server Actions ni helpers de servidor.
 *
 * Subpaths: `/lib`, `/schemas`, `/ui`, `/actions`, `/auth`, `/admin`.
 */
export * from './lib';
export * from './schemas';
